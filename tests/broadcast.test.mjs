import fs from'node:fs';import assert from'node:assert/strict';const entries=JSON.parse(fs.readFileSync('data/broadcasts.json')),tasks=JSON.parse(fs.readFileSync('data/broadcast-tasks.json'));let n=0;const check=(v,msg)=>{assert.ok(v,msg);n++};check(tasks.length===10,'ten tasks');check(new Set(tasks.map(t=>t.id)).size===tasks.length,'unique');for(const q of tasks){check(entries.some(e=>e.id===q.entryId),'entry reference');check(q.correct>=0&&q.correct<q.choices.length,'valid answer');for(const text of [q.question,...q.choices.map(c=>c.japanese)])check(!/[一-龯々]/.test(text.replace(/\[[^\]]+\]/g,'')),'reading coverage');check(q.choices.every(c=>c.chinese&&c.japanese),'bilingual options')}console.log(JSON.stringify({broadcastChecks:n,status:'passed'}));
