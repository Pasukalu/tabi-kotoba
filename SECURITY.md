# 凭证与上传检查

- `.env.local` 和其他实际环境配置不进入 Git；`.env.example` 只包含空凭证与公开配置名称。
- DeepSeek 与语音服务凭证只由服务端读取，不使用 `NEXT_PUBLIC_*` 保存密钥。
- GitHub 自动检查不需要 DeepSeek Key，也不运行付费在线对话测试。不要为这个检查工作流配置模型凭证。
- 上传前运行 `npm run check:secrets`，检查当前被追踪的文件、实际暂存内容、Git 历史及提交说明。脚本可在本机读取环境配置中的凭证进行内存比对，但不输出其值，不把值放入进程参数。
- 扫描包括常见 API Key、GitHub Token、私钥及本机实际凭证匹配；它是额外检查，不能保证识别所有服务的凭证格式。不要提交调试日志、浏览器存储备份或真实个人信息。
- 如果发现真实凭证曾进入仓库，先停止上传、撤销或轮换凭证，再处理提交历史；仅在最新版删除并不足以清除历史。

GitHub 仓库只保存源码。部署所需凭证在部署平台私有环境配置中单独设置，不复制到仓库或构建产物。

仓库内已准备 GitHub 自动检查流程；只有上传后实际运行成功，才能标记 GitHub 检查通过。本机检查不等于远端检查结果。
